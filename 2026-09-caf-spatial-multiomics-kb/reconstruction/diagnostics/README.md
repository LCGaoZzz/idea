# 已执行的数学诊断，不是论文复现

代码为本次自写，使用 Python 标准库和确定性合成对象，不需要作者数据。`run_receipt.json` 保存真实命令、退出状态、代码与输出哈希。结果：18项检查通过；未生成任何论文生物学结果。

| 诊断 | 实际输出 | 能说明什么 |
|---|---|---|
| 同一细胞被两个CAF邻域覆盖 | 邻域出现2次、唯一细胞1个 | CAF邻域计数和唯一细胞数是不同量 |
| 两类组成闭合 | 相关系数 −1 | 比例负相关可由和为1产生，不能直接解释为排斥 |
| NMF 对角重标度 | 乘积不变；W行间欧氏距离2.8284变2.0100 | 分解乘积不变不能保证图聚类几何不变 |
| 局部/全局组成比 | 同为局部20%，背景2%与20%时为10与1 | 富集值不是绝对浸润 |
| 合成60例按结局早晚分组 | log-rank χ²=71.3511，p≈2.99×10⁻¹⁷ | 结局参与分组可人为制造强组间差异 |
| 同一合成序列按预设交替索引分组 | χ²=0.1224，p≈0.7264 | 这是对照分组，不是真实独立临床验证 |

其他检查覆盖：同切片约束、中心是否纳入、物理半径与固定近邻区别、空邻域、重复标识、非有限坐标、最近五个距离均值、目标数不足、log-rank标签防护。测试的覆盖范围不等于生产级空间算法或生存分析库的全面验证。

运行：`python diagnostics/diagnostic_examples.py`。

这些反例不证明作者实际上犯错；要判断实际论文是否受影响，必须读取其矩阵、活动代码与统计设计。特别是NMF已有适当归一化或生存时间未参与聚类时，不能把反例当作对论文的指控。
